# Firebase App Hosting configuration
# See https://firebase.google.com/docs/app-hosting/configure

# The runtime for the app
runtime: nodejs20

# Settings for the backend service (Cloud Run)
runConfig:
  minInstances: 0
  maxInstances: 10
  memory: 512Mi
  cpu: 1
